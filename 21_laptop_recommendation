// https://www.codechef.com/practice/course/1-star-difficulty-problems/DIFF1200/problems/LAPTOPREC


#include <bits/stdc++.h>

using namespace std;

int main() {
    // your code goes here
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        vector < int > a(n);
        for (int i = 0; i < n; i++) {
            cin >> a[i];
        }
        unordered_map < int, int > freq;
        vector < int > m;
        for (int x: a) freq[x]++;
        int count = 0;
        int maxfreq = 0;
        for (auto & p: freq ) {
            if (p.second >maxfreq) {
                maxfreq = p.second;
                m.clear();
                m.push_back(p.first);

            }else if(p.second==maxfreq){
                m.push_back(p.first);
            }
            

        }
       
       
        if(m.size()==1){
            cout<<m[0]<<endl;
        }else{
            cout<<"CONFUSED"<<endl;
        }
       


    }

    // we use clear if maxfreq changes then clear that element and insert new max element
